# -*- coding: utf-8; mode: snippet -*-
# name: pwntools intro
# key: pwni
# --
from pwn import *

if args.GDB:
    target = gdb.debug("$1")
elif args.REMOTE:
    target = remote("nc ", "port")
else:
    target = process("$1")$0

elf = context.binary = ELF("$1")
# libc = ELF("libc-.so")